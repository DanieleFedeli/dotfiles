set -g default-terminal "xterm-256color"

# Action keys
unbind C-b
set-option -g prefix C-f
set-option -g repeat-time 0

# Key bindinds
bind r source-file ~/.tmux.conf \; display "Reloaded config!"

bind o run-shell "open #{pane_current_path}"
bind -r e kill-pane -a

# Window splitting
bind \\ split-window -hc "#{pane_current_path}"
bind - split-window -vc "#{pane_current_path}"

# Vim-like pane switching
bind -r i select-pane -U
bind -r k select-pane -D
bind -r j select-pane -L
bind -r l select-pane -R


# Window managment
bind -r a kill-window

#### COLOUR
# look'n feel
set-option -g status-fg cyan
set-option -g status-bg black
set -g pane-active-border-style fg=colour166,bg=default
set -g window-style fg=colour233,bg=default
set -g window-active-style fg=colour12,bg=default
set-option -g history-limit 64096
set -sg escape-time 10

# default statusbar colors
set-option -g status-style bg=colour,fg=colour136,default

# default window title colors
set-window-option -g window-status-style fg=colour244,bg=colour234,dim

# active window title colors
set-window-option -g window-status-current-style fg=colour166,bg=default,bright

# pane border
set-option -g pane-border-style fg=colour235 #base02
set-option -g pane-active-border-style fg=colour136,bg=colour235

# message text
set-option -g message-style bg=colour235,fg=colour166

# pane number display
set-option -g display-panes-active-colour colour33 #blue
set-option -g display-panes-colour colour166 #orange

# clock
set-window-option -g clock-mode-colour colour64 #green

# allow the title bar to adapt to whatever hostyou connect to
set -g set-titles on
set -g set-titles-string "#T"

# Powerline
source ~/.tmux.powerline.conf 
